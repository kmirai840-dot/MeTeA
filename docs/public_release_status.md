# 限定テスト公開の反映状況（2026-09-10）

## 今回反映した内容

- GitHub mainへ実装コミット`2bf57d0`をpushし、リモートの一致を確認。
- StreamlitのSecretsにGoogle認証・許可メール・運営者メール・Neon接続を設定。
- OAuthの戻り先を公開URLの`/oauth2callback`に設定。ローカルの戻り先は変更していない。
- 既存APIキーを維持。APP_ENVをtest、認証をgoogle、保存先をpostgresqlに設定。
- 休止していた公開アプリを起動。

## 公開環境で確認できたこと

- 公開URLのGoogleログイン画面と保存情報・利用目的の説明が表示された。
- 許可済みの本人GoogleアカウントでOAuth往復に成功し、ホーム画面を表示。Neonの同じ内部ID 2でログインを確認した。
- 公開フォームから基本情報の確認用下書きを送信し、NeonのID 2に保存されたことをDB側で確認した。
- Streamlit管理画面からRebootした後、再アクセスして同じ下書きが復元されることを確認した。
- 運営者画面でID 1（未連携の既存データ）とID 2が分かれて表示され、ID 2の件数と保存内容が表示された。
- 運営者ZIP出力ボタンからブラウザのdownloadイベントを確認した。端末のDownloadsフォルダにはファイルを確認できず、公開版から取得したZIPの中身の再照合は未実施。出力内容の自動テストは別途成功済み。
- 確認用の下書きだけを、ID・本人ID・内容の一致を条件に削除し、DBから消えたことを確認した。

## To-Be：未確認・残作業

- 2人目の実Googleアカウントの指定、必要な招待設定、本人によるログインと利用者間の閲覧・変更拒否の確認。
- 公開ZIPを端末へ保存した後の内容確認。
- ブラウザアプリ全体を終了した後の再アクセス確認（タブ再アクセス・サーバー再起動とは区別）。
- 利用者に案内する連絡方法と、データ取扱説明の運営者確認。

コード・設定の公開反映は完了したが、複数の実テストユーザーでの全条件達成とはまだ判断しない。ローカルと公開環境は同じNeon DBを利用するため、今後の開発テストは専用の一時スキーマで行う。


## 2026-09-10：接続切れ時のエラー修正

公開利用中、初期化後の接続close内rollbackでOperationalErrorが出る報告を受けた。接続が切れた直接の契機（休止・ネットワーク等）は未特定。コードには接続プールの貸出時検査がなく、rollback失敗時にプールへ返却されない不具合があった。
貸出時にConnectionPool.check_connectionを実行し、切れた接続の後片付けは元の例外を上書きせず、必ず返却してプールに破棄・補充させる。ログイン初期化中に通信エラーが残る場合は再試行案内を表示する。書込み・commitの自動再送やSQLiteへの切替はしない。
Neonへの実接続で、この検証プロセス自身の接続だけを切断し、貸出時の交換・切断後のclose・次のSELECT成功を確認。業務データは変更していない。公開環境の長時間休止後の再発有無は継続して確認が必要。
参考：[psycopg接続プールの生存確認](https://www.psycopg.org/psycopg3/docs/advanced/pool.html#connection-quality)。


## 初回招待コード方式への変更

メール事前登録から、Googleログイン後の初回招待コード入力へ変更する。通常利用者の所有者チェックと運営者専用権限を維持する。As-Is/To-Beと確認範囲は[招待コード設計](invite_code_spec.md)を参照。


### 求人詳細のimportエラー対応

求人詳細のimport中にKeyErrorが発生した報告を受けた。画面遷移ごとのimportlib.reloadとファイル監視による再読込みが競合し得るため、通常importへ変更し、fileWatcherTypeをnone、runOnSaveをfalseにした。開発時もコード更新後はアプリを再起動する。同時import24回の検証は成功。公開での同一エラーの再発有無は区別して確認する。


### 招待コード方式の公開設定

Google側を本番環境、MeTeA側をinvite_codeに切替済み。メール事前登録の作業は不要になった。共有相手にはURLと初回招待コードを渡す。コードはGitに記録せず、配布用ファイルを別途用意した。実際の新規Googleアカウントでの確認は未実施。

公開アプリ再起動後、求人156のimport KeyErrorは再現せず、AI評価未完了の案内画面まで表示できた。既存利用者は招待コードの再入力なしで継続利用できた。


## AI結果の自動反映（2026年9月10日）

一覧・比較・詳細の処理中の自動確認と結果反映、未処理バッチの継続、評価中の追加変更の保持を実装。AIモデルと採点精度に関わる設定は維持。旧「完了後は自動更新しない」仕様は廃止。検証範囲・残件は[job_ai_matching_spec.md](job_ai_matching_spec.md)の今回のAs-Is/To-Beを参照。公開動作の確認済みとローカルテストを区別する。

反映記録：実装コミット `8ac8e03` をmainへpush済み。公開ログのUpdated appと再起動（2026-09-10 14:09 UTC）を確認し、求人詳細・保存済みAI結果を表示できた。回帰テストは `Ran 77 tests / OK (skipped=7)`、Python 93ファイルの構文検査成功。新規の実AI評価完了を公開ブラウザで待って自動切替を確認する試験は未実施。モデル速度の計測・モデル変更は行っていない。


白画面対策：認証・サービス読込前の案内、ページ読込案内、求人画面内の評価データ再利用、待ちなし時の不要なDB確認省略を追加。携帯レイアウトの不足箇所と未実装To-Beはoverall_spec.mdへ記録。実機対応完了や時間短縮の測定済みとはしない。

公開確認：`a85194e`を反映・再起動後、求人詳細の表示と、再読込直後の「MeTeAを読み込んでいます。接続とログイン状態を確認しています…」をブラウザで確認。回帰テスト `Ran 77 tests / OK (skipped=7)`、Python 93ファイル構文検査成功。処理時間の比較計測と携帯実機確認は未実施。


携帯・LINE共有：開閉式の携帯メニュー、比較表内の横スクロール、求人番号対応、ログイン前の外部ブラウザ案内を追加。幅別の画面部品検証は実施、LINE実機のログイン確認は未実施。詳細はoverall_spec.mdの最新As-Is/To-Beを参照。


2026-09-13：基本情報・希望条件を部分更新へ変更し、認証接続失敗時の入力消去を修正。入力スナップショット、同一本人に限る復元、下書き保存失敗時の案内を追加。原因の確認範囲・残る全体更新はself_discovery_spec.mdを参照。

検証記録：80件実行で成功（skipped=7）、94 Pythonファイル構文検査成功。ローカル実ブラウザで、基本情報の検証エラー・希望条件の項目変更と一時保存は全体起動回数が増えず、基本情報の保存成功で次へ進む場合のみ全体起動回数が増えることを確認。公開反映は利用者の未保存入力の確認後に実施する。


2026-09-13 公開反映：利用者の一時保存完了を確認してから、`70182eb`をmainへpushし、公開アプリを再起動した。公開の基本情報画面と「次へ」の表示を確認。本人データの再保存は行わず、部分更新・失敗復旧の操作試験は隔離テストで確認した。カレンダーの数字入力案は利用者の指示で撤回済みで、今回の公開コードに含めていない。


## 2026年9月13日：自分を知る全ページの部分更新と遷移

前項の「基本情報・希望条件のみ」の範囲を拡張する。対象は入口、基本情報、希望条件、価値観、就活の軸、職務経歴・スキル、登録内容の見直し（全5カテゴリ）。見た目・入力部品・ボタン名は変更しない。

| 観点 | As-Is（直前の実装） | To-Be（今回実装） |
|---|---|---|
| 入力・編集・一時保存 | 基本情報・希望条件だけfragment。価値観などは全体を再実行 | 全対象ページをfragment化。同じページ内の明示的な再描画も部分更新 |
| 価値観の順位選択、軸の追加・編集、職歴の手入力切替、見直しのモーダル内入力 | 操作で冒頭の認証・接続確認も再実行 | ページ内で再実行し、アプリ冒頭へ戻らない |
| 保存成功後の「次へ」・戻る | URL変更後に全体rerun | 遷移先をセッションにも一時保持してルーターで一度だけ消費。遅れて届く古いURLによる遷移取消を防ぐ |
| 保存失敗・入力エラー | 次画面へ進めない | 維持。保存成功した場合のみ遷移する |
| 登録内容の見直しモーダル（全カテゴリ） | 入力はダイアログ内更新。保存・キャンセルで全体更新 | 維持。Streamlit標準dialogを閉じて親画面を更新するための全体更新は必要 |

`ui/page_execution.py`でページ内再描画とページ移動を区別する。初回の全体描画中はfragmentスコープを指定できないため全体rerunへフォールバックする。保存時の所有者確認・利用停止チェックは維持。遷移先の一時状態は利用者切替・ログアウト時に他のセッション状態とともに消去する。

別ページへの移動、HTMLリンクによる移動、モーダルを閉じる操作、ブラウザ再読込・初回起動は部分更新の対象外。DB・AI処理そのものの待ち時間をなくす変更ではない。

確認状況：公開環境での複数回押下の厳密な再現条件・ログは未確認。通常条件の一回押下と、古いURL到着を模擬した遷移継続を検証する。公開ユーザーの実データはテストで書き換えない。

検証結果：回帰テスト80件の実行は成功（外部PostgreSQL専用の7項目は設定なしでスキップ）。追加の遷移テスト2件は成功。Python 96ファイルの構文検査成功。ダミー保存を使った実ブラウザで、基本情報→希望条件→価値観→就活の軸→職務経歴の各一回押下、価値観の順位選択・一時保存・必須エラー、軸の追加フォーム表示、職歴の手入力切替・文字入力、見直しの編集モーダル、入口表示、基本情報の戻るを確認。入力・編集時の全体起動回数が増えないことと、モーダルを閉じる時だけ全体起動が一回増えることを確認した。実際のAI応答時間、全モーダルの全保存パターン、公開環境での実利用者の再確認はこの検証に含めない。

公開反映：2026年9月13日、利用者の一時保存完了を確認後、e261222をGitHubへpushし、Streamlit Cloudを再起動した。公開URLの価値観画面で見出し・一時保存・保存して次へボタンの表示を確認。公開環境での入力・保存操作による実データ変更は行っていない。利用者による操作感と一回押下の再確認は未実施。


## 2026年9月13日：ホームの「次の一歩」

As-Is：保存状況に関係なく基本情報への固定案内。To-Be：期限が近い未完了予定→一時保存の再開→進行中応募・応募意思→自己理解の保存不足→求人登録／比較／振り返りの順で本人の保存状態から選択する。画面構成は維持し、AIは呼び出さない。ホーム表示時に再判定する。詳細な優先順・判定基準・更新タイミング・検証範囲は [home_next_step_spec.md](home_next_step_spec.md) を参照。


## 2026年9月13日：保存・遷移の速度改善

As-Is：ボタンの処理前に旧画面を再描画し、保存と副処理で接続・コミット・有効性確認を繰り返していた。To-Be：描画前の保存受付、サービス操作内の接続共有・一括コミット、回答の一括登録、同じGoogle本人の再登録処理省略へ変更。有効性確認・所有者チェック・保存失敗時の下書き保持は維持する。サービス保存は短縮したが、画面全体の最長3秒目標は未達。詳細・実測値・安全性・未確認事項は [save_transition_performance_spec.md](save_transition_performance_spec.md) を参照。

## 2026-09-13 公開反映の確認

044b389（ホーム案内）、8bfba5e（保存処理短縮）、2a5f439（求人一覧NameError修正）をmainへpushし、Streamlit Cloudを再起動した。本人から就活の軸の一時保存済みの回答を受けて実施。

確認済み：公開求人一覧の見出し・検索・AI推薦欄が例外なく表示。求人登録の方法選択画面も表示。公開ホームの「次の一歩」は「応募判断が未確定の求人があります」「求人を確認・比較する」と表示され、固定の基本情報案内が解消した。保存済み進捗がその間に更新されており、以前の就活の軸入力案内とは異なる。

未確認：公開環境の実ユーザーデータを上書きする保存速度測定は行っていない。最長3秒以内の目標は未達のまま。ローカル計測と条件はsave_transition_performance_spec.mdを参照。

## 2026-09-13 AI評価失敗の復旧

公開ログで求人156に構造化JSONのEOFが3回発生。出力上限拡張と完了判定を実装し、実APIで旧上限3,000を超える回答を確認。さらに従来の検証が検出した重大必須条件フラグの矛盾を、生成JSON Schemaでも禁止した。どの元項目が分類/判定のどちらに違反したかの詳細は記録しておらず、断定しない。

8856ef5をmainへpushしCloudを再起動。修正コードによる本人の失敗求人1件の再評価・保存が成功（3,703出力トークン、41.6秒）。公開求人詳細でも「AIマッチング評価が完了しました。」と総合評価・カテゴリ別結果の表示を確認。求人本文・プロフィールは未変更。専用4テストと既存採点関連11テスト成功。全件/他利用者/すべての将来回答の成功を保証するものではない。

## 2026-09-13 お知らせ掲載

5b616a2を公開反映・再起動し、ホームの「次の一歩」の上に、お知らせの日付・見出し・概要と詳細展開項目が表示されることを確認。詳細の開閉と390px/1440pxでの横あふれなしはローカルブラウザで確認。公開画面での詳細クリックの自動操作は要素取得に失敗し、開閉確認は未実施。掲載内容はoverall_spec.md参照。

## 2026-09-13 お知らせの配置変更

1acb2a2を公開反映・再起動し、ヘッダーのメインナビゲーション内に「お知らせ」、その横に「使い方」が表示されることを確認。右列は「次の一歩」から開始。ローカルブラウザの1440px/390px/320pxで開閉・欄内スクロール・ヘッダーと本文の横あふれなしを確認した。

## 2026-09-13 アプリ内切り替えの公開確認

13b53a2で共通v2ナビゲーションを導入、71c0b44で認証後に登録するよう修正。公開反映・再起動後、新しい接続の初回1クリックでホーム→自分を知る、続けて共通メニュー→求人登録へ切り替わることを確認。処理中案内の表示・完了後の消去を確認。ブラウザ操作の検証はCloud内のアプリ本体URL（/~/+/）で行った。利用者のフォーム保存・求人登録・削除は行っていない。

ローカル実ブラウザでは内部リンク・保存ボタン・ID付きリンクで文書リクエストが初回の1回に留まり、接続識別値と入力値が維持された。外部/別タブ/ダウンロード/Ctrlクリック/アンカーは通常動作を保持。回帰110件成功（7件スキップ）。全端末・すべての個別ボタンの実測はしておらず、初回ロード・外部認証・切断復旧や保存自体の待ち時間をゼロにはしていない。

## 2026-09-13 登録方法カードの折りたたみ公開確認

79451a9を公開反映・再起動。公開の求人登録画面でPDFを選択するとカードが閉じ、PDF入力欄と「登録方法を変更」が表示されることを確認。「登録方法を変更」でカードが再表示され、「選択カードを閉じる」で閉じる操作も確認。ファイル送信・AI実行・求人保存は行っていない。AppTest2件（PDF/貼り付け/URLの各分岐、再選択・入力保持、手動遷移）成功。

## 2026-09-13 雇用形態と通勤説明の公開確認

7718aee/c260345を公開反映・再起動。現在の本人の保存済み求人157の詳細で、「希望する雇用形態『正社員』と求人の雇用形態『正社員』が一致しています」の表示と、合っている点の同内容の重複解消を確認。電車移動時間の確認欄を開き、「アプリの最寄駅欄が未登録」「住所からの自動計算は未対応」の案内を確認。求人・プロフィール・評価点の変更、AI再実行は行っていない。

## 2026-09-13 勤務地住所からの通勤確認

5d7406bを公開反映・再起動。求人157の電車移動時間欄で、実際の勤務地住所の入力欄と経路確認の案内が表示されることを確認。公開DBへの時間保存・AI再評価・実際のGoogle経路検索は未実施。ローカルAppTestでは住所入力後の時間欄表示、保存、同値再保存時のAI抑制を確認。回帰107件を実行し失敗なし（環境依存7件スキップ）。As-Is/To-Beと保存互換性はjob_commute_spec.mdおよび関連設計書に記録。

## 2026-09-13 独立したツール・スキル欄

59a3a52を公開反映。事前に公開PostgreSQLへuser_skills表のみ追加し、既存データの更新は行っていない。公開再起動後、職務経歴・スキル入力画面および登録内容見直し画面で入力欄・保存ボタンの正常表示を確認。本人のスキルを代筆・保存する操作や本番AIの再評価は行っていない。回帰109件、失敗なし（7件スキップ）。自動テストでは保存・再取得・削除相当の空欄保存、別利用者アクセス拒否、同値時の無効化抑制、AI入力への伝達、運営者出力を確認。

## 2026-09-13 スキル選択式の公開確認

f949df9/ad9eb22を反映し再起動。仕事で使用・学習/個人活動で使用の複数選択欄と任意補足の表示を確認。公開の候補検索でJava、GASを確認。Python・SQL・Excel VBAを含む選択肢の保存復元、既存文章保持等の関連テスト5件成功。本人のスキルの選択・保存やAI実行は行っていない。

2026-09-13：9c6ff09反映・再起動後、公開の候補検索でExcel入力・Excel関数・Excel VBAの3項目のみ表示されることを確認。本人の保存操作は行っていない。旧詳細保持を含む関連テスト6件成功。

## 2026-09-13 スキル保存時のPostgreSQLエラー修正

As-Is：共通executeがINSERTにRETURNING idを追加していたが、user_skillsはuser_idを主キーとするためUndefinedColumnで保存できなかった。SQLiteの保存テストと公開表示確認では検出できなかった。To-Be：戻りID不要のexecutemanyで1件を保存し、自動RETURNINGを避ける。既存の所有者検証・トランザクション・変更時のみ評価無効化する処理は維持。

確認：公開DBと分離したランダムな一時PostgreSQLスキーマで、新規保存、更新、再取得、同値保存、別利用者へのアクセス拒否を実行し成功。テストスキーマは終了時に削除。SQLite/UIの関連6件も成功。本人の実データへスキルの代筆・保存は行っていない。


公開確認：c6929b7の反映・再起動後、スキル選択欄と保存ボタンの正常表示を確認。実際の保存検証は分離したPostgreSQLテストスキーマで行った。

## 2026-09-13 評価再試行の失敗を修正

公開ログの2回の失敗はAI回答の根拠原文の欠落。02a3c5dで生成時の制約と受信後検証を一致させ、根拠なしの採点を禁止したまま修正。本人の失敗求人157を修正後に1回再評価し、39.4秒で完了・保存成功（API completed、3,552出力トークン）。公開反映・再起動後、求人詳細にAI総合マッチ度と評価できた情報100%が表示され、失敗案内がないことを確認。関連検証15件成功。

## 2026-09-13 グラフ・評価一覧の分類表示

6b3761aを反映・再起動。求人157の公開詳細で、グラフの求人側応募必須条件の名称、一覧の4カテゴリ見出し、確定軸・仕事の進め方・職歴3内訳の行表示を確認。保存済み49項目の分類は希望27、軸13、職歴8、求人必須1、不明0。分類テスト3件成功。点数・保存データの変更やAI再評価は行っていない。

## 2026-09-13 一覧から求人詳細への読込改善

53ec1daを反映・再起動。公開一覧の会社名リンク（詳細カードと同じ遷移先）から求人157の詳細へ進み、求人詳細の折りたたみ・応募判断保存ボタンまで表示されることを確認。操作開始から次の状態確認まで約10.3秒（ツール呼出し間の時間を含むため描画時間の精密値ではない）。この確認時はAI再評価も進行中。DB読取部分は個別6.86秒から接続共有1.27秒へ短縮。全画面3秒以内は未確認。詳細データ・描画と通勤保存の関連6件成功。

## 2026-09-13 分類列・応募必須条件・ピボット選択

As-Is：一覧は分類別見出しで表示しており、各行の分類列を求める意図と相違。AI回答で応募必須条件が省略され未評価になった。Excelの選択肢は3区分だった。
To-Be：各行に分類列を追加。求人必須条件の回答を必須化し、カテゴリごとのグループ制約を追加。合意済みのExcel関数判定を適用。職務経歴・スキルに独立したピボットテーブルの選択肢を追加。
確認済み：スキル7件、分類3件、必須条件2件、AI回答4件成功。本人の求人157を実際に再評価し、46.4秒で完了・保存、必須条件100%を確認。その前の検証でグループ不整合を検出したため、生成スキーマも修正した。公開画面の反映確認はこの時点では未実施。

公開確認追記：d5c35ce・9cf87acを反映して再起動。職務経歴の候補検索で「ピボットテーブル」を独立表示、求人詳細で必須条件100%と各行の分類列を確認。コロンを含むPCスキル項目も「求人側の応募必須条件」と表示できた（項目名復元テストを追加し分類4件成功）。本人のスキル選択・保存の代行はしていない。ピボット使用経験は本人が必要に応じて選択する。

## 2026-09-13 確認結果の保存・更新

As-Is：企業確認項目には確認不要操作のみ。To-Be：確認結果の保存・編集・取り消し、求人の確認済み追加情報としての保持、当該求人のみ自動再評価を追加。確認結果は本人が入力する。
確認済み：関連7件成功。別の一時PostgreSQLスキーマで、既存判断を保持する列追加・新規保存・更新・他利用者からのアクセス拒否を検証し成功。実AIへ架空データを送り、転勤なし希望と転勤なしの一致、残業10時間上限に対する30時間の不一致を確認（10.1秒、実データ保存なし）。公開DBへ結果保存列を追加し、既存件数不変を確認。公開画面の確認は反映後に実施する。

# 火曜日の限定リリースに向けた速度改善

更新日：2026-09-13。採用方針は①「Streamlitを維持して取得処理を改善」。UI・Google認証・利用者別永続保存を維持する。

## As-Is / To-Be（今回実装）

| 対象 | As-Is | To-Be |
|---|---|---|
| ホーム | 応募・次の一歩・活動を別接続で取得 | 初期データを1操作・1接続で取得。描画前に接続を返却 |
| 応募一覧 | 応募ごとに求人・予定を取得 | 本人の求人・予定を一括取得し、応募ごとに振り分け |
| 求人一覧 | 紹介経路を求人ごとに取得し、絞り込み・カードで再取得 | 本人の紹介経路を一括取得し、同じ描画中だけ再利用 |
| 求人の複数値項目 | 求人ごとに業務内容・スキル等を取得 | 有効な本人の求人にJOINして一括取得。項目順を維持 |
| 求人詳細 | 確認記録と、その状態だけの一覧を別取得 | 確認記録を1回取得し状態一覧を生成 |

本人確認は各操作で維持する。全利用者共有の個人データキャッシュや長時間TTLは追加しない。保存した変更は次回取得で読み直す。描画・AI待機中にDB接続を保持しない。

## 計測した事実

公開環境と同じNeon DBをローカルPythonから読取計測。既存の本人データを使用し、入力内容は変更していない。数値はページ表示全体ではなく初期データ取得時間。

| 対象 | 変更前・2回目 | 変更後・2回目 | SQL回数（前→後） | 接続取得回数（前→後） |
|---|---:|---:|---:|---:|
| ホーム | 6.457秒 | 1.167秒 | 13→8 | 10→1 |
| 求人一覧 | 6.483秒 | 1.361秒 | 16→6 | 13→1 |
| 求人詳細 | 1.656秒 | 2.348秒 | 12→10 | 1→1 |

求人一覧の変更前計測は紹介経路の絞り込み候補＋先頭6件以内のカード取得を含む。経路で絞り込む追加取得は含まない。SQL回数はRepositoryのexecute呼出しで、接続確認・トランザクション設定等の内部通信は含めない。

変更後の各2回はホーム1.852/1.167秒、一覧1.086/1.361秒、詳細4.136/2.348秒。詳細は先行計測で1.439/1.445秒だったが変動があり、今回の計測から詳細の速度改善を断定しない。全画面1秒・最長3秒の達成は未確認。休止復帰、通信、認証、画面描画、AI実行時間は別途評価が必要。

## 検証

- 一括取得した紹介経路・求人・応募予定が従来の個別取得と一致すること。
- 2利用者の分離、更新の読直し、削除済み求人の除外。
- 求人・応募を20件追加しても取得SQL回数が増えないこと。
- 求人一覧の空・評価済み・失敗状態の描画、描画中の紹介経路再取得がないこと。
- 詳細の確認状態を保持し、描画前にトランザクションが終了すること。

回帰テスト163件（154成功、9スキップ）。追加の一括取得テストを含む利用者分離テスト19件成功。

## 今後のTo-Be（未実装）

③のReact等の画面＋Python APIへの分離は限定リリース後の候補。入力状態をブラウザで保持し、必要な部分だけ取得・更新する。既存の評価・保存ロジックを移す前に、認証と所有権の境界をAPI側に定義する。今回は移行を開始せず、実際のテスト利用で残る待ち時間を記録して判断する。


## 2026-09-13 追加実装：取得方法と保存時連携

描画の変更は採用しない。希望条件の非表示部品・監視、求人詳細の遅延描画、色・形・入力方式は今回変更しない。

| 対象 | As-Is | To-Be（今回の実装） |
|---|---|---|
| 職務経歴・スキル | 職歴ごとに詳細取得、スキルは別操作 | 職歴詳細をJOINで一括取得し、スキルと同じ操作で取得 |
| 登録内容を見直す・入口 | 全カテゴリの内容を取得して登録状況を判定 | 登録有無・件数だけを1つのSQLで取得 |
| 見直す・各カテゴリ | 選択カテゴリとサイドバーが別々に取得 | 選択カテゴリ＋登録状況を1操作で取得し、その描画中だけ共用 |
| 求人比較 | 全求人・全評価を取得、通勤を個別取得 | 選択した本人の求人・評価・通勤を一括取得。AI進捗監視も同じ対象に限定 |
| 求人詳細 | 本人の全評価・全応募判断を取得 | 当該求人だけ取得し、初期描画で確認記録を共用 |
| 応募管理・活動入口 | 表示のたびに応募判断と応募管理を同期 | 応募判断の保存と応募作成を1トランザクションで処理し、表示は読取のみ |
| 応募詳細モーダル | 一覧で取得済みの求人・予定を再取得 | 初回だけ一覧の情報を消費して共用。保存後・再実行は再取得 |
| 選考準備 | 初期項目を個別作成し、作成済みでも再取得 | 不足分を一括作成。競合時は既存回答を保持。作成済みなら取得1回 |
| 活動レポート | 全応募の求人・予定・履歴を取得して期間で絞る | 応募日で対象を決めて関連情報を取得。対象応募の期間外の選考履歴は保持 |
| 運営者データ | 全表を閲覧用とZIP用に2回取得、毎回ZIP生成 | 件数＋選択した表だけ取得。ZIPはクリック時だけ生成し再認可 |

個人データを共有キャッシュに保存しない。取得範囲に空リストを渡した場合は0件、指定なしは従来どおり本人の全件。利用者ID・所有者・論理削除の制限は維持する。応募作成は同一求人をロックして二重作成を防ぎ、途中失敗時は応募判断も含めてロールバックする。

### 追加計測（初期データ取得のみ）

ローカルPythonから同じNeon DB、同一本人データ（求人2件・応募1件）。各2回。SELECT以外を拒否して測定し、入力・評価を変更していない。変更前は[全画面監査](performance_audit_20260913.md)。下表は両方の2回目。

| 対象 | 前 | 後 | SQL回数 前→後 | 接続取得 前→後 |
|---|---:|---:|---:|---:|
| 職務経歴・スキル | 2.722秒 | 0.724秒 | 6→4 | 3→1 |
| 見直す・入口 | 4.159秒 | 0.542秒 | 15→2 | 5→1 |
| 求人比較 | 6.176秒 | 1.263秒 | 16→8 | 9→1 |
| 求人詳細初期取得 | 1.674秒 | 1.356秒 | 10→10 | 1→1 |
| 応募管理 | 6.289秒 | 0.903秒 | 13→5 | 9→1 |
| 活動レポート | 7.490秒 | 0.996秒 | 15→6 | 11→1 |

各1回目は職歴0.749秒、見直し0.558秒、比較1.288秒、詳細1.681秒、応募0.911秒、レポート0.999秒。見直しの職歴カテゴリは0.824/0.813秒（5 SQL・1接続）。単独の応募詳細は1.321/1.901秒で変動があり、速度向上を断定しない。モーダル初回は取得済み一覧情報を使うため、詳細の追加取得自体を省略する。

画面全体の1秒・最長3秒、AI応答、休止復帰、複数人同時操作時の上限を保証する測定ではない。変更したデータ取得の削減を確認した値である。

既存データ確認：有効な利用者の「応募する／他経路から応募する」で、対応する有効な応募管理レコードがない件数は0件。既存データの書換え・一括再評価は行っていない。過去データを後日移行する場合は表示時の同期を戻さず、専用の整合性確認・補完処理で扱う。

### 追加実装の検証

- 回帰174件：165成功・外部環境用9スキップ。
- 追補：一覧からの入口を含む応募モーダル3件、見直し入口＋全5カテゴリの描画1件（6ケース）成功。スキル領域の再実行判定は現行Streamlitで動くことを描画テストで確認。
- 実PostgreSQLのランダムな一時スキーマで11件成功。取得範囲、本人分離、保存後の反映、同時応募作成、失敗時の全体取消、初期準備の既存回答保持、レポートの期間集計、運営者の権限解除・遅延ZIPを検証。テストスキーマは終了時に削除し、publicの入力データは変更していない。
- 管理対象のPython 152ファイルの構文検査成功。公開再起動後の確認は[公開状況](public_release_status.md)に記録する。
- 公開確認で一覧→モーダルの変数参照ミスを検出し、62e2c25で修正。入口の回帰テストを追加し、今回変更したPython全ファイルの未定義変数チェック（Ruff F821/F822/F823）も成功。

## 2026-09-13 取得改善後の画面遷移監査

- 対象：前回の取得改善で変更した画面。UI・採点・保存方式の追加変更は行わない。
- 実際のapp.pyを一時SQLite・ログイン済み相当の同一セッションで実行。見直し入口と5カテゴリ、職歴、求人一覧→詳細→2求人比較→一覧、応募一覧・予定・活動、応募詳細、選考準備の個別・入口、活動レポート、設定、再訪の22ケースを確認する。さらに準備3種類の切替と設定からホームへ戻るボタンを実行する。認証とAIキューだけをテストで置換し、本番データやAI APIには触れない。
- 検出：日付がNULLの未完了予定を持つ応募から選考準備を開くと、日付をHTML化する際にAttributeErrorになった。入口の一括取得自体ではなく、取得した未入力値の表示処理が原因。
- 修正：予定日未入力は既存表記「日程未定」へ変換して表示。入力済み日付は維持する。テストに未入力日付の表示と準備切替を含め、再発を検出する。
- 公開ブラウザ：ホームの予定リンク→予定結果モーダル→閉じる→選考準備入口→企業別準備→準備種類切替を確認。公開入力を代筆して保存する操作と手動AI再評価は行わない。
- 自動検証はtests/test_changed_page_navigation.py。画面表示・遷移の検証であり、全入力組合せや通信切断時の動作を網羅したものではない。

検証結果：22遷移ケース＋準備3種類の切替＋設定の戻る操作が成功。予定・結果モーダル関連3テスト、変更ファイルの未定義変数検査も成功。公開でも運営者への入口・設定への戻りを確認した。
